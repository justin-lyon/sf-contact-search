public with sharing class ContactSearchAuraService {
  public static ContactRepository contactRepo = new ContactRepository();

  // TODO: Write an AuraEnabled method that uses ContactRepository to return a List of Contacts
}